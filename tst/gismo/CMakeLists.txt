# G# Add component test subdirectories
# add_subdirectory(Common)  # Temporarily disabled - conflicts with src Common
# add_subdirectory(Math)  # Temporarily disabled until Math module is implementedO Test Component Orchestration CMakeLists.txt
# This file manages all GISMO component tests following the source structure

# Add component test subdirectories
add_subdirectory(Common)
# add_subdirectory(Math)